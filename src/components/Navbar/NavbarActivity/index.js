import React from 'react'
import { StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { ButtonActivity } from '../../Buttons'

export const NavbarActivity = () => {
  return (
    <View style={{
      backgroundColor: 'red',
      marginBottom: 16
    }}>
      <ButtonActivity />
    </View>
  )
}

// const styles = StyleSheet.create({})
